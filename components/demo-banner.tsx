export function DemoBanner() {
  return (
    <div className="bg-warning/10 text-warning text-xs px-4 py-1.5 text-center border-b border-border">
      Môi trường thí điểm — dữ liệu trên trang là dữ liệu mẫu (seed) để demo, không phải số liệu chính thức của VAST/HTIC.
    </div>
  );
}
